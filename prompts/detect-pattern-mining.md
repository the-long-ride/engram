Detect repeated undocumented decisions across session summaries.

Flag only patterns repeated at least the configured threshold and not already
covered by the memory index.

When the pattern describes a repeatable sequence of actions, especially one
formed from known rules plus project knowledge, propose it as a workflow/skill.
When it describes a correction or constraint, propose it as a rule. When it
describes a stable fact or decision, propose it as objective knowledge.

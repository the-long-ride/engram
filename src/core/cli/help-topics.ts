/** Detailed command help shown by `engram help <topic>`. */
export type CommandTopicHelp = {
  title: string;
  summary: string;
  useCases: string[];
  examples: string[];
  notes?: string[];
  aliases?: string[];
};

export const COMMAND_TOPICS: Record<string, CommandTopicHelp> = {
  '--version': topic('engram --version', 'Print the current Engram CLI version.', ['Check which build is installed before reporting an issue.'], ['engram --version', 'engram -v'], ['The runtime VERSION is generated from package.json during build.'], ['version', '-v']),
  init: topic('engram init', 'Create or reconcile memory roots and install compact Codex skillset instructions by default.', ['Start using Engram in a repository.', 'Create a global-only memory store without installing `.agents/.engram` in the current workspace.', 'Repair missing Engram-owned files and folders after upgrades.', 'Choose a workspace-specific global memory path.', 'Choose whether normal saves default to workspace, global, or both.', 'Give agents terse Engram operating instructions immediately.', 'Configure shared global memory through a Git remote.', 'Create `.agents/.engram` as a submodule when a team wants memory versioned separately.'], ['engram init', 'engram init --scope workspace', 'engram init --scope both --global-path ~/Documents/engram', 'engram init --global-only --global-path ~/Documents/engram', 'engram init --no-skillset', 'engram init --skillset all', 'engram init --no-global', 'engram init --global-path ~/Documents/engram', 'engram init --submodule', 'engram init --global-remote https://github.com/example/team-engram.git'], ['Fresh workspace installs default normal saves to `both`; if no global memory is configured yet, writes fall back to workspace until a global path exists.', 'When global memory is configured, init creates or selects a user default profile for that global root.', 'Default skillset target is `codex`; existing human-authored instruction files are skipped.', '`--global-only` writes a user-level pointer to the global folder, sets default saves to global, and skips local skillset installation.', 'Repeated init preserves memory files, hashes, changelog data, and existing save targets while refreshing generated scaffolding.', 'Interactive init prompts for submodule setup, optional global path, then optional global remote.']),
  help: topic('engram help', 'Show command help. Add a topic for deeper command-specific guidance.', ['Learn a command before running it.', 'Find examples for aliases, save flows, or operational commands.'], ['engram help', 'engram help set-role', 'engram -h save-session', 'engram save -h'], ['The overview stays short; topic help explains intent and examples.'], ['h', '-h']),
  entry: topic('engram entry', 'Show runtime configuration, memory roots, and global Git state.', ['Debug which global memory folder is active.', 'Confirm configured vs detected global Git branch.', 'Check whether live sync, roles, or rule variants are enabled.'], ['engram entry', 'engram e'], ['Use this before changing Git remotes or diagnosing unexpected memory routing.'], ['e']),
  profile: topic('engram profile', 'Manage isolated global memory profiles for company, personal, and team contexts.', ['Create separate global memory roots so company and personal memory do not share one pool.', 'Set a user default profile for uninitialized folders.', 'Set a workspace default profile for initialized repositories.', 'Run a one-off command against another profile with `--profile <name>`.', 'Merge one profile into another while reporting duplicate candidates.'], ['engram profile create personal --global-path ~/Documents/engram-personal --use', 'engram profile create company --global-path ~/Documents/engram-company', 'engram profile use company --workspace', 'engram --profile personal load "my preferences"', 'engram save --profile personal --scope global knowledge "My side project uses pnpm."', 'engram profile merge company personal --dry-run', 'engram profile merge --from-profile company --to-profile personal'], ['Profiles select the active global memory root; workspace and global remain the two memory scopes.', 'When a workspace default profile is set, it wins over the user default in that folder.', 'If `--profile` selects a different profile than the workspace default, workspace memory is disabled for that command to avoid cross-profile reads or writes.', 'Profile registries live in the user config directory, while workspace configs store only a default profile name.', 'Profile merge copies active `rules/`, `skills/`, and `knowledge/` memories from the source profile into the target profile and skips duplicates unless `--force` is passed.'], ['pf']),
  'update-global-folder': topic('engram update-global-folder', 'Update the configured global memory folder and optionally move an existing global root.', ['Point Engram at a new global memory folder without moving old files.', 'Rename the global root folder while preserving memory files, Git history, hashes, and sidecars.', 'Move global memory from an explicit old path when the active config is stale or overridden.', 'Let slash/chat users say natural phrases such as "set global memory path to ..." or "move global folder from ... to ...".', 'Replace an empty/generated destination scaffold with the moved root.', 'Refuse to overwrite a destination that already contains memory or user files.'], ['engram update-global-folder ~/Documents/engram-global', 'engram ugf ~/Documents/engram-renamed --move-from-path ~/Documents/engram', 'engram set global memory path to ~/Documents/engram-global', 'engram move global folder from ~/Documents/engram to ~/Documents/engram-renamed', 'engram update-global-folder F:\\engram\\new-global --move-from-path F:\\engram\\old-global'], ['Without `--move-from-path`, the command updates saved config and reconciles the target folder; existing memory stays where it is.', 'Natural phrases normalize to `update-global-folder`; supported verbs include update, set, change, move, and rename when followed by global folder/path/root wording.', 'With `--move-from-path`, Engram moves the whole old root before updating config. If the destination is missing, it renames the folder so `.git`, custom files, hashes, indexes, and sidecars are preserved.', 'If the destination exists with only generated scaffold files, Engram clears that scaffold and moves the old root into place.', 'If the destination already contains real memory or user files, the command fails before moving anything to avoid overwrites.', '`ENGRAM_GLOBAL_DIR` overrides saved config; when it points somewhere else, the command reports a warning.'], ['ugf']),
  completion: topic('engram completion', 'Print shell completion scripts for Tab suggestions.', ['Enable command suggestions in bash, zsh, or PowerShell.', 'Discover subcommands and common flags faster.'], ['source <(engram completion bash)', 'source <(engram completion zsh)', 'engram completion powershell', 'engram c bash'], ['The command prints the script; add it to your shell startup file when you want it loaded every session.'], ['c']),
  upgrade: topic('engram upgrade', 'Recommend the latest package update and refresh generated Engram assets.', ['Get a quick, copyable npm update recommendation.', 'Refresh initialized workspace HELP.md and generated workspace skillsets without a separate maintenance command.', 'Preview global memory and global skillset refreshes before writing.', 'Refresh Engram-owned global agent rules after an Engram release.', 'Limit the refresh to memory, global skillsets, or one target agent.'], ['engram upgrade', 'engram upgrade --plan', 'engram upgrade --latest', 'engram upgrade --global-skillsets-only', 'engram upgrade --target codex', 'engram up --plan'], ['Default output stays conservative and shows the quick package update command.', 'After an npm package update, normal Engram commands run a quiet one-time auto-upgrade for already-initialized roots; pass `--no-auto-upgrade` or set `ENGRAM_NO_AUTO_UPGRADE=1` to skip it.', '`engram upgrade` refreshes existing generated workspace skillset files and preserves human-authored files.', '`engram init` also refreshes generated workspace HELP.md and skillsets as part of scaffold reconciliation.', '`--self` attempts `npm install -g @the-long-ride/engram@latest` from the CLI, then still recommends running `engram upgrade` again under the new version.', '`--plan` and `--dry-run` do not write files.'], ['up']),
  save: topic('engram save', 'Propose one durable memory and save it only after A/B/C approval.', ['Capture a user correction as a rule.', 'Capture an objective project fact as knowledge.', 'Capture a repeatable procedure as a workflow/skill.', 'Attach role tags with `--role` or `--roles` so routing can filter frontend, backend, docs, or security memory.', 'Preview related existing memories that may be dependencies or duplicates before saving.', 'Override the configured save target for a single save with `--scope workspace`, `--scope global`, or `--scope both`.', 'Save into another profile global root with `--profile <name> --scope global`.', 'Let Engram auto-detect the type from concise text.'], ['engram save "Always use pnpm in this repo"', 'engram save --scope workspace rule "Keep this repo-local"', 'engram save --scope both knowledge "The app deploys through Vercel"', 'engram save --profile personal --scope global knowledge "My side project uses pnpm."', 'engram save rule --role frontend "Never hand-roll spacing; use design tokens"', 'engram save workflow "When releasing, run tests, update changelog, tag version"'], ['For long sessions with several candidates, use `engram save-session`; if you decline that suggestion, normal save captures the best single candidate.', 'Normal saves use the configured save target. Fresh installs default to `both`; `workspace`, `global`, and `both` are literal targets.', 'Save previews may report related memories with suggested `depends_on` or duplicate cleanup; these are advisory and require human follow-up.', 'When `--profile` differs from the workspace default profile, Engram defaults the save to that profile global root so the workspace is not touched.'], ['s', 'rule', 'skill', 'workflow', 'knowledge']),
  'save-session': topic('engram save-session', 'Propose multiple memories from a long session summary or agent brainstorm.', ['Mine a long human-agent interaction for several rules, knowledge facts, and workflows.', 'In AI agent chat, let the LLM define concise candidates from the current conversation before passing them to Engram.', 'Use `--query-level <n>` to ask an agent to include up to n recent human-agent chat sessions when it has access to them.', 'Read a transcript or summary from `--file transcript.md`.', 'Accept all candidates or only selected numbers such as `A 1,3`.', 'Use `--accept-all` when the human explicitly wants every agent-recommended candidate saved without a second reply.', 'Let agents rerun with `DEPENDS_ON` or `UPDATE` when Engram reports related memories before writing.'], ['engram save-session', 'engram ss', 'engram save-session --query-level 3', 'engram ss -a last 50 sessions', 'engram save-session --file transcript.md', 'engram save-session --file transcript.md --accept-all', 'engram save-session --role frontend "TYPE: rule | TEXT: Always run tests before release."', 'engram ss --scope workspace', 'engram ss --scope both "TYPE: rule | TEXT: Always run tests before release."', 'engram ss -a --scope workspace "TYPE: rule | TEXT: Always run tests before release."', 'engram ss -a --scope workspace "TYPE: rule | TEXT: OAuth rotation follows release foundations. | DEPENDS_ON: release-foundation | LEVEL: advanced"', 'engram ss -a --scope workspace "TYPE: knowledge | TEXT: Invoice retries use exponential backoff. | UPDATE: invoice-retry-baseline"'], ['Agents should provide one candidate per line using `TYPE: ... | TEXT: ...`.', 'Candidates may add `DEPENDS_ON: memory-id`, `LEVEL: advanced`, or `UPDATE: memory-id` fields after `TEXT`.', '`--query-level` must be a positive integer; agents should not invent unavailable chat history.', '`--accept-all` is explicit approval for save-session only; do not add it unless the human requested it.', 'When `--accept-all` reports related memories before writing, no file was saved yet; agents should brainstorm a restructured candidate set and rerun with `DEPENDS_ON` or `UPDATE`.', '`engram ss -a` is the compact shortcut for `engram save-session --accept-all`; `engram ss -a last 50 sessions` normalizes to `engram save-session --query-level 50 --accept-all`.', 'Save-session uses the configured save target unless `--scope workspace`, `--scope global`, or `--scope both` overrides it.'], ['ss']),
  observe: topic('engram observe', 'Capture a sanitized raw note in the non-indexed inbox, then optionally mine it through save-session.', ['Store session notes without turning them into active memory yet.', 'Redact sensitive values and remove prompt-injection-like lines from raw notes.', 'Use `--propose` to immediately convert the sanitized note into approved memory proposals.', 'Use `--file` for transcripts or agentmemory exports you want to inspect first.'], ['engram observe "We fixed auth retries by refreshing tokens before expiry."', 'engram observe --file session.md', 'engram observe --file session.md --propose', 'engram observe --propose --accept-all "TYPE: knowledge | TEXT: The auth middleware refreshes tokens before expiry."'], ['Inbox files live under `inbox/` and are not indexed as memories.', '`--propose` routes through save-session, so normal approval still applies unless the human explicitly passed `--accept-all`.'], ['o']),
  'take-control': topic('engram take-control', 'Explore existing workspace guidance with agent help and consume it as Engram memory.', ['Convert AGENTS.md, CLAUDE.md, Cursor rules, memory-bank files, notes, and similar guidance into approved Engram memories.', 'Preview source selection with `--plan` before any LLM candidate step.', 'Scan top-level rules, skills, workflows, knowledge, notes, memory-bank, and project-memory folders, including .md and .txt files.', 'Use optional @the-long-ride/markdown-them to convert document files such as PDF, DOCX, HTML, RTF, and ODT into Markdown before extraction.', 'Use repeated `--file`, `--dir`, and `--include` flags to combine sources.', 'Use `--exclude`, `--max-sources`, and `--max-chars` to keep scans token-safe.', 'Skip unchanged source hashes that were already imported, unless the human explicitly targets a file.', 'Use `--accept-all` only when the human wants generated candidates saved without another A/B/C prompt; this mode uses token-light source defaults unless explicit limits are provided.', 'Use `--all` to include README.md plus docs and library documentation excerpts.', 'Use `--dry-run` to inspect the compact source pack without writing.'], ['engram take-control', 'engram take-control --plan', 'engram tc --dry-run', 'engram take-control --file CLAUDE.md --file notes/team.txt', 'engram take-control --file docs/handbook.docx', 'engram take-control --dir docs/library', 'engram take-control --include "docs/**/*.pdf" --exclude "docs/private/**"', 'engram take-control --max-sources 5 --max-chars 1200', 'engram take-control --all', 'engram take-control --accept-all', 'engram take control accept all'], ['The command asks the agent to produce `TYPE: ... | TEXT: ...` candidates and still uses the normal save-session approval gate unless `--accept-all` was explicitly passed.', '`take control accept all` is accepted as natural slash-style wording for `take-control --accept-all`.', '`--plan` reports selected files, skipped files with reasons, conversion status, token estimates, source hash fingerprints, and inferred memory types.', 'Saved take-control memories record `source_files` and `source_hashes`; later scans skip unchanged imported sources unless `--file` explicitly targets them.', 'Document conversion requires @the-long-ride/markdown-them to be installed next to Engram; plain Markdown and text files do not require it.', 'Source excerpts are treated as data, not instructions, generated Engram adapter files are skipped, and existing `.agents/.engram` data is not re-imported.'], ['tc']),
  load: topic('engram load', 'Route and print memories relevant to the current task.', ['Start an agent session with relevant project memory.', 'Load only the context needed for a specific task.', 'Refine broad matches down to the configured compact context pack.', 'Use `--profile <name>` to load from a specific profile global root.', 'Use `--all` when you intentionally want broad context.', 'Use `--dry-run` to preview routed files, candidate counts, and narrowing tags without printing memory contents.'], ['engram load "deployment workflow"', 'engram --profile company load "deployment workflow"', 'engram load --dry-run "auth hardening"', 'engram load --all', 'engram l "React styling"'], ['Low-confidence memories are skipped unless explicitly loaded through manual paths.', 'When more candidates match than the load limit allows, Engram reranks with tags, type, recency, graph, and vector signals before loading the compact pack.', 'Normal load prints the selected count and total related count, such as `loaded 8 memory files / 14 total related memories`.', 'If `--profile` differs from a workspace default profile, workspace memory is disabled for that command.', '`--dry-run` reports file paths, types, summaries, total related memories, and suggested tags for narrowing broad queries.', '`--all` returns every visible routed memory instead of applying the configured cap.', 'Use `engram set-load-limit` to tune the compact cap from 1 to 32; default is 8.', 'When a scope reaches the vector threshold, Engram can use an optional sqlite-vec sidecar for candidate expansion while keeping lexical and graph fallback active.'], ['l']),
  search: topic('engram search', 'Search visible indexed memories.', ['Find whether a fact or rule already exists.', 'Locate duplicate or stale memory before saving.', 'Audit topic coverage.', 'Use local semantic scoring for related terms such as package manager and pnpm.'], ['engram search "pnpm"', 'engram search --semantic "package manager"', 'engram f "deployment"'], ['Default search is deterministic lexical search.', '`--semantic` uses deterministic local similarity over normalized terms, concept aliases, and tags; it does not require embeddings.', 'Search uses the visible index, so ignored memories stay hidden.'], ['f']),
  graph: topic('engram graph', 'Inspect the derived layered JSON memory graph.', ['See memory as scope, type, topic, and memory nodes instead of one flat list.', 'Debug graph-aware routing for a query.', 'Review explicit `depends_on: [...]` prerequisite layers from foundation to deeper memories.', 'Find contradiction candidates that may indicate wrong or stale memory.', 'Regenerate `memory.graph.json` with `--rebuild`.'], ['engram graph', 'engram graph "deployment"', 'engram graph --rebuild "pnpm"'], ['The graph is derived from approved Markdown memories and can be rebuilt at any time.', 'Memories can declare `depends_on: [base-memory-id]` in frontmatter; load keeps prerequisites in the compact route before dependent memories.', 'Contradiction candidates are advisory; archive wrong memory only after review.'], ['g']),
  verify: topic('engram verify', 'Verify memory hashes for workspace and global scopes.', ['Detect hand-edited or tampered memory files.', 'Run before committing memory changes.', 'Check only one scope when debugging.'], ['engram verify', 'engram verify workspace', 'engram vf global'], ['Hash mismatches make `load` skip unsafe content.'], ['vf']),
  'rebuild-index': topic('engram rebuild-index', 'Rebuild memory indexes from Markdown files.', ['Recover after index files were deleted.', 'Refresh indexes after approved manual cleanup.', 'Debug missing search/load results.', 'Refresh eligible sqlite-vec sidecars after schema changes.'], ['engram rebuild-index', 'engram rebuild-index workspace', 'engram ri global'], ['Invalid memory files are skipped during rebuild.', 'Vector sidecars are skipped when the scope is below threshold or sqlite-vec is unavailable.'], ['ri']),
  repair: topic('engram repair', 'Report invalid memory files that rebuild-index would skip.', ['Find malformed Markdown memories that are missing from search/load.', 'Check memory health after manual file edits or imports.', 'Review exact schema errors before fixing a file.'], ['engram repair', 'engram repair workspace', 'engram rp global'], ['Repair is read-only for now; fix or archive the reported file, then run `engram rebuild-index`.'], ['rp']),
  audit: topic('engram audit', 'List visible memories with optional author, stale, and confidence filters.', ['Find old memories that may need review.', 'Show memories owned by a teammate.', 'Inspect low-confidence candidates.'], ['engram audit', 'engram audit --stale', 'engram audit --author dev@example.com', 'engram a --low-confidence'], ['Audit reports metadata rows, not full memory content.'], ['a']),
  health: topic('engram health', 'Summarize memory health metrics.', ['Check memory coverage and quality trends.', 'Spot unhealthy memory stores before sharing.', 'Prepare for team review.'], ['engram health', 'engram he'], ['Use with `quality-check` for deeper quality signals.'], ['he']),
  'quality-check': topic('engram quality-check', 'Evaluate visible memory quality and potential issues.', ['Find vague or low-value memories.', 'Improve memory before exporting to agents.', 'Review memory hygiene during maintenance.'], ['engram quality-check', 'engram qc'], ['Quality checks are advisory; they do not write files.'], ['qc']),
  stats: topic('engram stats', 'Show counts, scope mix, and author ownership for visible memories.', ['Confirm a save increased memory count.', 'Compare workspace and global memory volume.', 'See team memory ownership by author.', 'Check whether ignore rules are hiding entries.'], ['engram stats', 'engram st'], ['Stats respect visibility filters and include the former team-dashboard ownership summary.'], ['st']),
  deduplicate: topic('engram deduplicate', 'Detect similar or duplicate visible memories.', ['Find repeated rules before they confuse agents.', 'Clean up after imports or team merges.', 'Prepare memory for a release.'], ['engram deduplicate', 'engram deduplicate --semantic', 'engram dd'], ['Default matching uses strict token overlap.', '`--semantic` uses deterministic local similarity over normalized terms and tags; it does not require embeddings.'], ['dd']),
  export: topic('engram export', 'Export visible memory to agent instruction formats or a JSON bundle.', ['Generate AGENTS.md, CLAUDE.md, or Cursor rules.', 'Share a reviewable bundle.', 'Feed memory into an agent without runtime integration.'], ['engram export --format agents-md', 'engram export --format claude-md', 'engram x --format cursorrules'], ['Exports skip ignored or unsafe memory.'], ['x']),
  import: topic('engram import', 'Import an Engram or agentmemory JSON bundle through the approval gate.', ['Review memory from another workspace.', 'Move memory between machines.', 'Merge team memory with human oversight.', 'Convert agentmemory export JSON into proposed Engram Markdown memories.'], ['engram import bundle.json', 'engram import --source agentmemory agentmemory-export.json', 'engram import --max 10 agentmemory-export.json', 'engram im team-memory.json'], ['Imports should never bypass approval.', 'agentmemory imports auto-detect memories, semantic memories, procedural memories, lessons, and insights when present.'], ['im']),
  archive: topic('engram archive', 'Move wrong or superseded memory out of active routing after approval.', ['Remove stale or wrong memory from `load` without deleting its history.', 'Let an AI agent propose cleanup while the human keeps the final approval gate.', 'Preserve archived Markdown under `archive/YYYY-MM-DD/`.'], ['engram archive use-pnpm-for-installs --reason "Repo migrated to npm."', 'engram archive workspace:rules/use-pnpm-for-installs.md', 'engram ar knowledge/old-deploy-fact.md'], ['Archive is a move, not a hard delete.', 'Archiving a workspace memory also archives its matching global copy when one exists.', 'Run `engram graph` or `engram quality-check` first to find likely contradiction candidates.'], ['ar']),
  benchmark: topic('engram benchmark', 'Measure graph-aware retrieval hit rate for a small JSON case file.', ['Compare routing changes before and after graph tuning.', 'Create regression tests for important project questions.', 'Check whether expected memories appear in the top routed set.'], ['engram benchmark eval/cases.json', 'engram bm memory-cases.json'], ['Case files are arrays like `[{"query":"deploy workflow","expect":["skills/deploy.md"]}]`.', 'Benchmark is read-only and reports hit@<current load limit>.'], ['bm']),
  ignore: topic('engram ignore', 'Manage ignore rules for memory visibility.', ['Hide private or noisy memory from routing.', 'Check whether a path is ignored.', 'Add project-specific ignore patterns.'], ['engram ignore status', 'engram ignore check knowledge/private.md', 'engram ignore add private/**', 'engram ig status'], ['Ignore rules affect search, load, export, and stats.'], ['ig']),
  'set-role': topic('engram set-role', 'Configure active developer roles for context routing.', ['Load frontend-only memory during UI work.', 'Load backend and security memory during auth work.', 'Clear roles when you want unfiltered routing.'], ['engram set-role frontend design', 'engram set-role backend security', 'engram set-role docs', 'engram set-role'], ['When roles are set, memories with non-matching `role: [...]` frontmatter are skipped. With no active roles, role filtering is disabled.'], ['sr', 'role', 'roles', 'set-roles']),
  'set-save-target': topic('engram set-save-target', 'Configure where normal save writes by default.', ['Keep memory local to the current repository with workspace.', 'Save only portable personal or team memory with global.', 'Write approved memories to both workspace and global at the same time.', 'Inspect the current target with status.'], ['engram set-save-target status', 'engram set-save-target workspace', 'engram set-save-target global', 'engram set-save-target both'], ['Fresh installs default to `both`.', '`global` requires configured global memory. `both` writes workspace and global when global memory is configured, and otherwise falls back to workspace until a global path exists.', 'Per-command `--scope workspace|global|both` overrides this default for save, save-session, take-control, observe propose, and agentmemory import.'], ['save-target']),
  'set-load-limit': topic('engram set-load-limit', 'Configure the compact load cap.', ['Keep normal agent startup compact in small or noisy memory stores.', 'Raise the cap when a workspace has many tightly related foundational memories.', 'Lower the cap when you want stricter token control.', 'Inspect the current limit with status.'], ['engram set-load-limit status', 'engram set-load-limit 12', 'engram ll 6', 'engram set-load-limit reset'], ['The value must be an integer from 1 to 32.', 'Default is 8.', '`engram load --all` remains the explicit way to bypass the cap for broad context.', 'Normal load reports both the selected count and total related memories so agents can decide whether to narrow or restructure memory.'], ['ll', 'load-limit']),
  'set-rule-variant': topic('engram set-rule-variant', 'Tune how strict rule memories render for agents.', ['Use strict for lower-tier models that need stronger guardrails.', 'Use light or balanced for top-tier models so rules do not limit their reasoning power.', 'Turn variants off when compact output is preferred.'], ['engram set-rule-variant strict', 'engram set-rule-variant balanced', 'engram set-rule-variant off', 'engram rv status'], ['Engram always saves all three rule variants; this command only changes the rendered output for load/export/sync.', 'When variants are off, Engram renders balanced rule wording by default.'], ['rv', 'rule-variant']),
  'resolve-conflicts': topic('engram resolve-conflicts', 'Preview or resolve Engram-owned Git conflicts in memory files.', ['Inspect memory merge conflicts safely.', 'Resolve deterministic Engram-owned conflicts.', 'Avoid touching unrelated project files.'], ['engram resolve-conflicts --dry-run', 'engram resolve-conflicts', 'engram rc --dry-run'], ['Use `--dry-run` first when reviewing team memory conflicts.'], ['rc']),
  'install-hooks': topic('engram install-hooks', 'Install Git hooks for local Engram integrity checks.', ['Verify workspace memory before commits.', 'Resolve Engram memory conflicts after pulls.', 'Add guardrails to team workflows.'], ['engram install-hooks', 'engram ih'], ['Human-authored hooks are preserved unless generated by Engram.'], ['ih']),
  'install-skillset': topic('engram install-skillset', 'Generate workspace or global agent-specific instruction files and slash adapters.', ['Install AGENTS.md instructions for Codex-compatible agents.', 'Install Gemini context files, including the current Gemini-compatible Antigravity surfaces.', 'Install slash command adapters.', 'Configure MCP-capable clients.', 'Install global user rules or skills so an AI agent loads Engram global memory in new workspaces.'], ['engram install-skillset all', 'engram install-skillset codex', 'engram install-skillset gemini', 'engram install-skillset slash', 'engram install-skillset --global codex', 'engram install-skillset --global claude', 'engram install-skillset --global gemini', 'engram install-skillset --global all', 'engram is list'], ['Existing human-authored files are skipped unless `--force` is used.', '`agents-md` is the generic AGENTS.md fallback for unlisted AGENTS.md-compatible agents.', '`gemini` also covers current Antigravity 2.0, Antigravity CLI, and Antigravity IDE Gemini-compatible paths while Google keeps those surfaces in flux.', 'The dedicated `antigravity` and `antigravity-cli` targets remain hidden compatibility aliases for now.', 'Global installs append one Engram managed block at the end of shared instruction files and record successful installs for future `engram upgrade` refreshes.', 'Global Claude writes `~/.claude/CLAUDE.md` and `~/.claude/skills/engram/SKILL.md`.', 'Some agents expose global rules only through application settings; those targets are reported as skipped/manual.'], ['is']),
  'clone-memory': topic('engram clone-memory', 'Clone active memory Markdown between workspace and global scopes.', ['Promote repository-local memories into portable global memory.', 'Bring global memories into a workspace for local review or offline use.', 'Preview the copy set before writing with `--dry-run`.', 'Route clone candidates through proposal-first save-session approval with `--restructure`.', 'Overwrite destination copies only when the human explicitly passes `--force`.', 'Let agents accept natural wording such as "clone workspace memory to global".'], ['engram clone-memory workspace global', 'engram clone-memory global workspace --force', 'engram clone-memory workspace global --dry-run', 'engram clone-memory workspace global --restructure', 'engram clone-memory workspace global --restructure --dry-run', 'engram clone-memory workspace global --restructure --accept-all', 'engram cm workspace global', 'engram clone workspace memory to global'], ['Only active `rules/`, `skills/`, and `knowledge/` Markdown files are cloned; archive, inbox, proposals, indexes, hashes, and vector sidecars are regenerated or left out.', 'Normal clone is mechanical: the destination memory frontmatter is rewritten to the destination scope, and destination hashes, index, graph, and eligible vector sidecar are refreshed.', '`--restructure` is proposal-first: verified source memories become save-session candidates for the target scope, using the same approval, related-memory hints, and accept-all restructuring rules as save-session.', '`--force` cannot be used with `--restructure`; use numbered approval, `--dry-run`, or human-requested `--accept-all` instead.', 'Agents may use `--accept-all` with `--restructure` only when the human supplied it.', 'Existing destination files are skipped unless `--force` is passed on the mechanical clone path.', 'Source files must pass Engram hash verification before they are cloned or proposed. Run `engram verify` first if a file is reported unsafe.', 'Use `engram sync` after cloning global memory when you want to pull, commit, or push the global Git remote.'], ['cm']),
  sync: topic('engram sync', 'Sync global memory with Git and refresh enabled live-sync targets.', ['Pull/push shared global memory.', 'Refresh generated AGENTS.md or Cursor rule targets.', 'Keep team memory current after approved saves.'], ['engram sync', 'engram sy'], ['Live sync targets must be enabled in config.'], ['sy']),
};

export function detailForTopic(rawTopic: string): CommandTopicHelp | undefined {
  const topic = rawTopic.toLowerCase().replace(/^engram\s+/, '').trim();
  return COMMAND_TOPICS[topic] ?? Object.values(COMMAND_TOPICS).find((item) => item.aliases?.includes(topic));
}

function topic(title: string, summary: string, useCases: string[], examples: string[], notes?: string[], aliases?: string[]): CommandTopicHelp {
  return { title, summary, useCases, examples, notes, aliases };
}
